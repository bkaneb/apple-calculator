export interface IColors {
  app: {
    background: string;
  };
  calculator: {
    background: string;
    header: {
      buttons: {
        close: string;
        reduce: string;
        fullScreen: string;
      };
    };
  };
}
